using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class DialoguePopup : MonoBehaviour
{

    [SerializeField] private GameObject TextBox;

    private void BeginLineOfDialogue()
    {
        // Create text box
        // Size text box according to amount of text
        // Place a little flashy arrow at the bottom of the text box
        // Start a coroutine to make the letters pop up
            // If the player clicks while dialogue is showing up, make it all pop up immediately
            // If they click once it's all shown up, prompt the next dialogue
    }
}
